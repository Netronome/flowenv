file: tests/include/README.txt

Files here are samples of includes that flowenv libraries depend on and
projects need to provide.  They are intended to allow the tests to run
only.  Projects should not include files from this path directly, but
may use these samples as examples for their files.
